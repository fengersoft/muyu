{
  "name": "muyu",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . muyu --platform=win32 --arch=x64 --overwrite --icon=./icons/app.ico  --ignore=node_modules --out=./build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@electron/get": "^3.0.0",
    "electron": "^28.2.1"
  }
}
